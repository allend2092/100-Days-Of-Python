# This is a sample Python script.


# Press the green button in the gutter to run the script.
if __name__ == '__main__':

    # ask user for input
    year = int(input("Which year do you want to check?"))

    # Multiply input by 100 to avoid python math turning int into float
    year *= 100

    # Calculate if the year is a leap year or not
    if year % 4 == 0:
        # print("year is divisible by 4 and divisible by 100, LEAP")
        if year % 100 == 0:
            # print("year is evenly divisible by 100, NOT LEAP!")
            if year % 400 == 0:
                # print("year is evenly divisible by 400, LEAP!")
                print("It is a leap year!")
            else:
                print("Not a leap year!")
        else:
            print("Not a leap year!")
    else:
        print("Not a leap year!")


# See PyCharm help at https://www.jetbrains.com/help/pycharm/
